{% extends 'layouts/ares/fleet.html.twig' %}

{% block content %}
	<div class="grid grid-cols-4 gap-x-4">
		{{ include('components/fleet/spy_reports.html.twig') }}
		{% if focused_spy_report is not null %}
			{{ include('components/fleet/spy_report.html.twig', {
				'spy_report': focused_spy_report,
				'place': focused_spy_report.place
			}) }}
		{% else %}
			{{ include('molecules/common/default.html.twig') }}
			{{ include('molecules/common/default.html.twig') }}
		{% endif %}
	</div>
{% endblock content %}
