<img {{ attributes.defaults({
	src: asset('images/avatars/' ~ player.avatar),
	alt: 'Avatar de ' ~ player.name,
	class: 'w-32 h-32 shrink-0 mx-auto rounded-full bg-dark' ~ player.faction.identifier,
 }) }} />
