<div class="component player rank">
	<div class="head skin-2">
		<h2>Statut diplomatique</h2>
	</div>
	<div class="fix-body">
		<div class="body">
			{% for statement in diplomacy_statements|keys %}
				{% set factions = faction.colorLink|filter((v, k) => k != 0 and k != faction.id and v == statement) %}
				<h4>{{ diplomacy_statements[statement] }}</h4>

				{% for faction_id in factions|keys %}
					<div class="player faction color{{ faction_id }}">
						<a href="{{ path('embassy', {'faction': faction_id}) }}">
							<img src="{{ asset('media/faction/flag/flag-' ~ faction_id ~ '.png') }}" alt="" class="picto">
						</a>
						<span class="title">{{ get_faction_info(faction_id, 'government') }}</span>
						<strong class="name">{{ get_faction_info(faction_id, 'officialName') }}</strong>
					</div>
				{% endfor %}

				{% if factions|length == 0 %}
					<p>Aucune faction</p>
				{% endif %}
			{% endfor %}
		</div>
	</div>
</div>
