<div class="component size2">
	<div class="head skin-2">
		<h2>Carte tactique</h2>
	</div>
	<div class="fix-body">
		<div class="body">
			<div class="tactical-map">
				<svg class="sectors" viewBox="0, 0, 750, 750" xmlns="http://www.w3.org/2000/svg">
					{% for sector in sectors %}
						<polygon data-id="{{ sector.identifier }}"
						class="ally{{ sector.faction.identifier ?? 0 }} {{ (sector.faction.identifier ?? 0) != 0 ? 'enabled' : 'disabled' }}"
						points="{{ galaxy_configuration.sectorCoord(sector.identifier, map_scale, 0) }}"
						/>
					{% endfor %}
				</svg>
				<div class="number">
					{% for sector in sectors %}
						{% set i = sector.identifier - 1 %}
						<span id="sector{{ sector.identifier }}" class="ally{{ sector.faction.identifier ?? 0 }}" style="top: {{ galaxy_configuration.sectors[i].display[1] * map_scale / 1.35 }}px; left: {{ galaxy_configuration.sectors[i].display[0] * map_scale / 1.35 }}px;">
							{{ sector.identifier }}
						</span>
					{% endfor %}
				</div>
			</div>
		</div>
	</div>
</div>
